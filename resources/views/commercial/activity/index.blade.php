<x-layouts.app title="فعالیت‌ها"><div class="mb-7"><p class="section-kicker">Audit trail</p><h1 class="page-title">فعالیت‌های اخیر</h1><p class="page-subtitle">رویدادهای عملیاتی ثبت‌شده برای باشگاه جاری، بدون نمایش داده‌های حساس</p></div><x-card class="mb-5"><form class="flex gap-2"><input class="field" name="search" value="{{ request('search') }}" placeholder="جست‌وجوی نوع رویداد"><x-button type="submit" icon="search">جست‌وجو</x-button></form></x-card>@if($logs->isEmpty())<x-empty-state title="رویدادی ثبت نشده" description="فعالیت‌های مهم پس از انجام عملیات اینجا ظاهر می‌شوند."/>@else<div class="table-shell"><table class="data-table"><thead><tr><th>رویداد</th><th>کاربر</th><th>موضوع</th><th>زمان</th></tr></thead><tbody>@foreach($logs as $log)<tr><td><x-badge>{{ $log->event }}</x-badge></td><td>{{ $log->user?->name??'سامانه' }}</td><td>{{ $log->auditable_type?class_basename($log->auditable_type).' #'.$log->auditable_id:'—' }}</td><td>{{ $log->created_at->format('Y-m-d H:i') }}</td></tr>@endforeach</tbody></table></div><div class="mt-5">{{ $logs->links() }}</div>@endif</x-layouts.app>
