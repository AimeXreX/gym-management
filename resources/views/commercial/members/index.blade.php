<x-layouts.app title="اعضا"><div class="mb-7 flex flex-wrap items-end justify-between gap-3"><div><h1 class="page-title">اعضای باشگاه</h1><p class="page-subtitle">پرونده اعضا، وضعیت و عملیات گروهی.</p></div><div class="flex flex-wrap gap-2"><a class="btn-secondary" href="{{ route('tenant.members.export',request()->query()) }}">خروجی CSV</a><a class="btn-secondary" href="{{ route('tenant.members.import') }}">ورود CSV</a><x-button :href="route('tenant.members.create')">عضو جدید</x-button></div></div><x-card class="mb-5"><form class="grid gap-3 sm:grid-cols-[1fr_12rem_auto]"><x-input name="search" label="جست‌وجو" :value="request('search')"/><x-select name="status" label="وضعیت"><option value="">همه</option><option value="active" @selected(request('status')==='active')>فعال</option><option value="inactive" @selected(request('status')==='inactive')>غیرفعال</option></x-select><x-button type="submit" class="self-end">اعمال</x-button></form></x-card><div class="table-shell"><table class="data-table"><thead><tr><th>عضو</th><th>کد</th><th>موبایل</th><th>شعبه</th><th>وضعیت</th></tr></thead><tbody>@forelse($members as $m)<tr><td><a class="font-black text-indigo-600" href="{{ route('tenant.members.show',$m) }}">{{ $m->full_name }}</a></td><td>{{ $m->membership_code }}</td><td>{{ $m->mobile }}</td><td>{{ $m->branch?->name }}</td><td>{{ $m->status==='active'?'فعال':'غیرفعال' }}</td></tr>@empty<tr><td colspan="5"><x-empty-state title="عضوی پیدا نشد" description="فیلترها را تغییر دهید."/></td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $members->links() }}</div></x-layouts.app>
