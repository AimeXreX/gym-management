<x-layouts.app title="جست‌وجو"><div class="mb-7"><p class="section-kicker">Global search</p><h1 class="page-title">جست‌وجوی سراسری</h1><p class="page-subtitle">جست‌وجوی امن در داده‌های عملیاتی باشگاه جاری</p></div><x-card><form class="flex gap-2"><input autofocus class="field text-base" name="q" value="{{ $q }}" minlength="2" placeholder="نام، کد عضویت، موبایل، کلاس یا مرجع پرداخت"><x-button type="submit" icon="search">جست‌وجو</x-button></form></x-card><div class="mt-5 space-y-5">@if(mb_strlen($q)<2)<x-empty-state icon="search" title="حداقل دو حرف وارد کنید" description="نتایج بر اساس ماژول‌های فعال دسته‌بندی می‌شوند."/>@elseif(empty($results)||collect($results)->flatten(1)->isEmpty())<x-empty-state icon="search" title="نتیجه‌ای پیدا نشد" description="عبارت دیگری را امتحان کنید."/>@else @foreach($results as $category=>$items)@if($items->isNotEmpty())<x-card><h2 class="text-xs font-black muted">{{ $category }}</h2><div class="mt-3 divide-y" style="border-color:var(--line)">@foreach($items as $item)<a href="{{ $item['url'] }}" class="flex items-center justify-between py-3"><div><p class="text-sm font-bold">{{ $item['title'] }}</p><p class="mt-1 text-xs muted">{{ $item['subtitle'] }}</p></div><x-icon name="chevron-left" class="muted"/></a>@endforeach</div></x-card>@endif @endforeach @endif</div></x-layouts.app>
