<x-layouts.app title="پرداخت‌ها"><div class="mb-7"><p class="section-kicker">Collections</p><h1 class="page-title">پرداخت‌ها</h1><p class="page-subtitle">مبالغ وصول‌شده و ابطال‌های مالی با سابقه کامل</p></div>@if($payments->isEmpty())<x-empty-state icon="wallet" title="پرداختی ثبت نشده" description="پرداخت‌ها از صفحه عضو ثبت می‌شوند."/>@else<div class="table-shell"><table class="data-table"><thead><tr><th>عضو</th><th>مبلغ</th><th>روش</th><th>تاریخ</th><th>وضعیت و عملیات</th></tr></thead><tbody>@foreach($payments as $p)<tr><td><a href="{{ route('tenant.members.show',$p->member) }}" class="font-bold">{{ $p->member->full_name }}</a></td><td>{{ number_format((float)$p->amount) }}</td><td>{{ $p->payment_method }}</td><td>{{ $p->paid_at->format('Y-m-d H:i') }}</td><td><x-badge :tone="$p->status==='completed'?'success':'neutral'">{{ $p->status }}</x-badge>@if($p->status==='completed')<form method="POST" action="{{ route('tenant.payments.void',$p) }}" class="mt-2 flex gap-1">@csrf<input name="reason" class="field !min-h-8 !py-1 text-xs" placeholder="دلیل ابطال" required minlength="5"><button class="text-xs font-bold text-red-600">ابطال</button></form>@else<p class="mt-1 text-xs muted">{{ $p->void_reason }}</p>@endif</td></tr>@endforeach</tbody></table></div><div class="mt-5">{{ $payments->links() }}</div>@endif</x-layouts.app>
