<x-layouts.app title="پذیرش سریع"><div class="mb-7"><p class="section-kicker">Front desk</p><h1 class="page-title">ثبت ورود و خروج</h1><p class="page-subtitle">کد عضویت، موبایل، نام یا توکن کارت عضو را وارد کنید.</p></div><div class="grid gap-5 xl:grid-cols-[1fr_.75fr]"><div><x-card><form method="GET"><label class="label" for="search">جست‌وجوی عضو</label><div class="flex gap-2"><input id="search" autofocus class="field text-lg" name="search" value="{{ request('search') }}" placeholder="کد عضویت یا موبایل" autocomplete="off"><x-button type="submit" icon="search">جست‌وجو</x-button></div></form></x-card><div class="mt-4 space-y-3">@foreach($matches as $member)<x-card><div class="flex flex-wrap items-center gap-4"><strong>{{ $member->full_name }}</strong><span class="text-xs muted">{{ $member->membership_code }}</span><form method="POST" action="{{ route('tenant.attendance.check-in',$member) }}" class="mr-auto">@csrf<input type="hidden" name="method" value="{{ request('search')===$member->public_token?'qr':(request('search')===$member->membership_code?'member_code':'manual') }}"><x-button type="submit" icon="activity">ثبت ورود</x-button></form></div></x-card>@endforeach</div></div><div class="space-y-5"><x-card><div class="flex justify-between"><h2 class="font-black">مراجعه‌های باز</h2><x-badge tone="warning">{{ $openVisits->count() }}</x-badge></div><div class="mt-4 divide-y">@forelse($openVisits as $a)<div class="flex items-center justify-between py-3"><div><p class="text-sm font-bold">{{ $a->member->full_name }}</p><p class="text-xs muted">ورود {{ $a->checked_in_at->format('H:i') }}</p></div><form method="POST" action="{{ route('tenant.attendance.check-out',$a) }}">@csrf<x-button type="submit" variant="secondary">ثبت خروج</x-button></form></div>@empty<p class="py-6 text-center text-sm muted">مراجعه بازی وجود ندارد.</p>@endforelse</div></x-card><x-card><h2 class="font-black">ورودی‌های اخیر</h2><div class="mt-4 divide-y">@foreach($recent as $a)<div class="flex justify-between py-3 text-sm"><span>{{ $a->member->full_name }}</span><time class="muted">{{ $a->checked_in_at->format('H:i') }}</time></div>@endforeach</div></x-card></div></div></x-layouts.app>
