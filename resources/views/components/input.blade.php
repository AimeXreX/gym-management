@props(['label'=>'','name','type'=>'text','value'=>null,'hint'=>null])
<div><label for="{{ $name }}" class="label">{{ $label }}</label><input id="{{ $name }}" name="{{ $name }}" type="{{ $type }}" value="{{ $type!=='password'?$value:'' }}" @if($errors->has($name)) aria-invalid="true" aria-describedby="{{ $name }}-error" @endif {{ $attributes->except(['label','value','hint'])->class(['field','!border-red-500'=>$errors->has($name)]) }}>@if($hint&&!$errors->has($name))<p class="mt-2 text-xs muted">{{ $hint }}</p>@endif @error($name)<p class="mt-2 text-xs font-semibold text-red-600" id="{{ $name }}-error">{{ $message }}</p>@enderror</div>
