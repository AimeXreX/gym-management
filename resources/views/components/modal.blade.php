@props(['name','title'])
<div x-data="{open:false}" @open-modal.window="if($event.detail==='{{ $name }}')open=true" x-show="open" x-cloak @keydown.escape.window="open=false" class="fixed inset-0 z-[70] grid place-items-center bg-slate-950/60 p-4 backdrop-blur-sm"><section role="dialog" aria-modal="true" aria-labelledby="{{ $name }}-title" @click.outside="open=false" class="premium-card w-full max-w-lg p-6 shadow-2xl"><div class="flex items-center justify-between"><h2 id="{{ $name }}-title" class="text-lg font-black">{{ $title }}</h2><button class="icon-btn !size-9" @click="open=false" aria-label="{{ __('ui.close') }}"><x-icon name="x" :size="16"/></button></div><div class="mt-5">{{ $slot }}</div></section></div>
