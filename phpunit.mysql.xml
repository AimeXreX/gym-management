<?xml version="1.0" encoding="UTF-8"?>
<!--
  MySQL verification configuration. Unlike phpunit.xml.dist (which pins an
  in-memory SQLite database), this config intentionally does NOT override
  DB_CONNECTION / DB_DATABASE so the test suite runs against the MySQL
  connection defined by .env.testing. APP_ENV stays "testing" so the
  application behaves exactly like a normal test run (e.g. CSRF bypass).
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         failOnRisky="true"
         failOnWarning="true">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Architecture">
            <directory>tests/Architecture</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_KEY" value="base64:akh1gGlYcToqksK6c9yCSn9L9afGSnX5EY3NvTUms98="/>
        <env name="APP_DEBUG" value="true"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="SESSION_ENCRYPT" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="FILESYSTEM_DISK" value="local"/>
    </php>
</phpunit>
